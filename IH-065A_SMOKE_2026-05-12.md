# IH-065A Smoke - Move-level Audit Provenance

Date: 2026-05-12

## Scope
Persist durable, public-safe provenance for newly created agent moves without exposing raw private prompts.

## Changes verified
- `Move` now has nullable provenance columns so legacy moves remain readable:
  - `provider`
  - `model`
  - `modelVersion`
  - `agentEngineVersion`
  - `systemPromptHash`
  - `userPromptHash`
  - `promptCommitHash`
- `getAgentMove` returns provider/model/version metadata and SHA-256 prompt commit hashes for provider responses and deterministic engine recovery/fallback paths.
- Match API move creation and tick-created RPS/TTT moves persist the new fields.
- Tournament audit export includes the new public-safe move fields and keeps `gates.realMoneyReady=false`.

## Verification
- `npm run db:generate` passed.
- `npm run lint` passed.
- `npm run build` with the default local env failed at page-data collection because `DATABASE_URL` was not Postgres-shaped, matching the repo's production guard.
- `DATABASE_URL='postgresql://user:pass@localhost:5432/interhouse' npm run build` passed.
- Re-verified 2026-05-12 17:15 EDT with one chained local gate: `npm run db:generate && npm run lint && DATABASE_URL='postgresql://user:pass@localhost:5432/interhouse' npm run build` passed. Build compiled, TypeScript passed, and generated all 16 static pages.

## Notes
No deployment, external messaging, credentials, or billing changes were made.
