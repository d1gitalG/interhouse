TASK: RPS Match Resolution Engine + Move API

Next.js 16 app, Prisma v7 + SQLite (better-sqlite3 adapter). Build is currently clean.

1. Create lib/rps-engine.ts with:
- resolveRPS(p1, p2): 'P1_WIN'|'P2_WIN'|'DRAW'  (ROCK beats SCISSORS, SCISSORS beats PAPER, PAPER beats ROCK)
- checkSeriesWinner(p1Score, p2Score, series: 'QUICK'|'BO3'|'BO5'): 'P1'|'P2'|null  (QUICK ends at 1, BO3 at 2, BO5 at 3)

2. Update app/api/matches/[matchId]/move/route.ts — after recording a move:
a. Check if BOTH participants have moved this round (query Move table by matchId + match.currentRound)
b. If both moved: resolve RPS, determine winner (isCreator=true = P1), increment winner MatchParticipant.score
c. Check checkSeriesWinner with updated scores. If series complete: set Match.status=COMPLETED, Match.winnerId, update AgentProfile wins/losses, call settleMatch from @/lib/credits if stakeMode=CREDITS
d. If series not complete: increment Match.currentRound
e. Return full updated match (participants with agent, moves, scores, winner info)

3. Verify app/api/matches/[matchId]/route.ts GET returns currentRound.

Import paths use @/lib/... Prisma client is at @/lib/prisma. Run npm run build when done to verify clean TypeScript.

When completely finished run: openclaw system event --text "Done: InterHouse backend match engine built" --mode now
